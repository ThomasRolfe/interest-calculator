import type { NextPage } from 'next'
import { FC } from 'react';

const Layout = ({children}: any) => {
    return (
        <div>
            {children}
        </div>
    )
}

export default Layout;